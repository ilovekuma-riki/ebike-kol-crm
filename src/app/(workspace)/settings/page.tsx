import { KeyRound, RefreshCcw, ShieldCheck } from "lucide-react"; import { PageHeader, Button } from "@/components/page-header"; import { stores } from "@/lib/demo-data"; import { StatusPill } from "@/components/status-pill";
export default function Settings(){return <div><PageHeader eyebrow="SYSTEM SETTINGS" title="系统设置" description="管理四个 Shopify 销售站、同步状态与内部权限。密钥只从服务端环境变量读取。"/><div className="p-5 md:p-8"><div className="mb-5 flex gap-1 border-b border-[#e1e5e8]">{["Shopify 站点","折扣码映射","成员与权限","自动化"].map((x,i)=><button className={`border-b-2 px-4 py-3 text-xs font-semibold ${i===0?"border-[#111418]":"border-transparent text-[#7a838b]"}`} key={x}>{x}</button>)}</div><div className="grid gap-4 md:grid-cols-2">{stores.map(s=><section className="rounded-[16px] border border-[#e1e5e8] bg-white p-5" key={s.code}><div className="flex items-start justify-between"><div className="flex gap-3"><span className="grid size-10 place-items-center rounded-[11px] bg-[#111418] font-mono text-xs font-bold text-white">{s.code}</span><div><h2 className="text-sm font-bold">{s.name}</h2><p className="mt-1 font-mono text-[9px] text-[#858d95]">{s.domain}</p></div></div><StatusPill tone="warn">{s.status}</StatusPill></div><div className="my-5 grid grid-cols-2 gap-2 rounded-[11px] bg-[#f5f7f8] p-3 text-[10px]"><div><span className="text-[#7b838b]">订单币种</span><b className="mt-1 block font-mono">{s.currency}</b></div><div><span className="text-[#7b838b]">最近同步</span><b className="mt-1 block">尚未连接</b></div></div><div className="flex items-center justify-between"><span className="flex items-center gap-2 text-[10px] text-[#707a83]"><KeyRound size={12}/>{`SHOPIFY_ACCESS_TOKEN_${s.code}`}</span><Button secondary><span className="flex items-center gap-2"><RefreshCcw size={11}/>同步</span></Button></div></section>)}</div><div className="mt-5 flex items-start gap-3 rounded-[14px] border border-[#dbe2e8] bg-white p-4"><ShieldCheck className="text-[#53728d]" size={18}/><div><b className="text-xs">密钥不会返回浏览器</b><p className="mt-1 text-[10px] leading-5 text-[#74808a]">请在 Vercel 或本地 .env 中配置。每站同步独立记录，单站失败不影响其他已完成站点。</p></div></div></div></div>}
